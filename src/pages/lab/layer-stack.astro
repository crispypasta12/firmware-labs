---
import LabLayout from "../../layouts/LabLayout.astro";
import LabShell from "../../components/LabShell.astro";
import LayerStack from "../../components/labs/LayerStack.astro";
---

<LabLayout title="Lab 10 · App / HAL / Driver / BSP — who owns what — Firmware Labs">
  <LabShell
    num="LAB 10"
    title="App / HAL / Driver / BSP — who owns what"
    tag="PDF Q6"
    why="<b>Where this bites you:</b> this question maps directly onto real BSP work — answer it with the layer names <i>and</i> a concrete example from your platform. Click each layer."
    takeaway='<b>The senior framing:</b> "The goal wasn&#39;t making one board work — it was separating board-specific config from generic driver logic so multiple platform variants shared the same firmware components."'
  >
    <LayerStack />
  </LabShell>
</LabLayout>
