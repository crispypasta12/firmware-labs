---
import LabLayout from "../../layouts/LabLayout.astro";
import LabShell from "../../components/LabShell.astro";
import DmaPingpong from "../../components/labs/DmaPingpong.astro";
---

<LabLayout title="Lab 04 · Circular DMA + half/full interrupts (ping-pong) — Firmware Labs">
  <LabShell
    num="LAB 04"
    title="Circular DMA + half/full interrupts (ping-pong)"
    tag="PDF Q8–Q9 · Q45"
    why='<b>Where this bites you:</b> UART/ADC DMA often works until load rises — expect "walk me through it" and "what goes wrong?" Toggle the slow CPU and watch the overrun happen: that&apos;s the failure mode to name unprompted.'
    takeaway="<b>The rule:</b> ping-pong only works if processing time per half &lt; fill time per half. Bugs to list: wrong address/length/width, missed flag clear, ISR/task index races, wraparound math, and cache coherency on MCUs with D-cache."
  >
    <DmaPingpong />
  </LabShell>
</LabLayout>
