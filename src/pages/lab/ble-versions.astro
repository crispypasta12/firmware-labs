---
import LabLayout from "../../layouts/LabLayout.astro";
import LabShell from "../../components/LabShell.astro";
import BleVersions from "../../components/labs/BleVersions.astro";
---

<LabLayout title="Lab 30 · BLE 4.0 → 5.4 — what arrived when — Firmware Labs" maxWidth={1000}>
  <LabShell
    num="LAB 30"
    title="BLE 4.0 → 5.4 — what arrived when"
    tag="Spec literacy"
    why='<b>Where this bites you:</b> dating features ("DLE is 4.2, 2M PHY is 5.0, subrating is 5.3") instantly signals depth. Click a version. Note: <b>CC2340 ships BLE 5.3</b> — connection subrating is its headline.'
    takeaway='<b>Two you must own for TI:</b> <b>Connection Subrating (5.3)</b> — a connected device skips most events by a negotiated factor but can ramp to full rate instantly; better than slave latency for "idle but must respond fast" products (earbuds, locks). <b>PAwR (5.4)</b> — periodic advertising with responses; thousands of nodes synchronized to one broadcaster — electronic shelf labels.'
  >
    <BleVersions />
  </LabShell>
</LabLayout>
